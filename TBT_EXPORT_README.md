# SafeNexus HSE TBT Export Upgrade

Existing TBT topics are retained. Added topic export actions:
- Copy / Share full TBT text
- PDF (.pdf)
- Word (.docx)
- Excel (.xlsx)

Dependencies already present in the project are reused where possible.
Run `flutter pub get`, then `flutter analyze`, then the Android build.
